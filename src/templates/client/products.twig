<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

<section class="h-full">
        <div class="container">
          <nav class="h-24 flex justify-between items-center">
                <span class="text-xl font-semibold">Store</span> 
                {% if SESSION.auth %}
                    <div>
                        <span class="px-5 py-1.5 cursor-pointer transition-all rounded-md border-[1px] border-red-400 text-red-400 hover:scale-105 hover:bg-gray-300 " >Logout</span>
                    </div>
                {% else %}
                    <div class="flex gap-5">
                        <a class="px-5 py-1.5 bg-gray-200 transition-all rounded-md border-[1px] hover:scale-105 hover:bg-gray-300  border-black" href="/signin" >Signin</a>
                        <a class="px-5 py-1.5 bg-gray-200 transition-all rounded-md border-[1px] hover:scale-105 hover:bg-gray-300  border-black"  href="/signup" >SignUp</a>
                    </div>
                {% endif %}
            </nav>
            <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-10 mdl:gap-4 lg:gap-10">
                {% for p in products %}
                    <div class="shadow-md rounded-md w-20">
                            <div class="">
                                <div class="w-64 h-64 overflow-y-hidden hover:shadow-md cursor-pointer">
                                                <div class="bg-white h-[100%]">
                                                    <img 
                                                    class=" w-full h-full object-cover" 
                                                    src='{{p.img}}'
                                                    alt="" />
                                                </div>
                                </div>
                                    <div class="py-4 flex flex-col gap-1 border-[1px] border-t-0 px-2">
                                                        <div class="flex items-center justify-between font-titleFont">
                                                        <h2 class="text-md text-primeColor font-bold">{{p.name}} </h2>
                                    </div>
                                </div>
                            </div>
                    </div>
                    {% endfor %}
        
            </div>
        </div>
    </section>
</body>
</html>